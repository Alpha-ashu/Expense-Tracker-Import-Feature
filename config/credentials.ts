// Centralized credentials and API keys
export const API_KEYS = {
  // Example: 'stripe': process.env.STRIPE_API_KEY,
};

export const INTEGRATION_SECRETS = {
  // Example: 'firebase': process.env.FIREBASE_SECRET,
};

// Add more as needed for integrations
